package com.musula.backend.drones.converter;

public class DroneConverters {
   /* public static final Function<PortalUser, PortalUserDTO> userConverter = portalUser -> {
        PortalUserDTO portalUserDTO = ModelMapperUtil.getInstance().map(portalUser, PortalUserDTO.class);
        return portalUserDTO;
    };*/

    private DroneConverters() {
    }
}
